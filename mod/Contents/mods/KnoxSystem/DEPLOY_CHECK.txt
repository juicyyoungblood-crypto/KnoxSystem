# DEPLOY CHECK — if in-game still shows old Analyze text like [mod:Heavy_Hit]

Workspace SoT (this is what Hermes edits):
  /opt/data/workspace/pz-system-apocalypse/mod/Contents/mods/KnoxSystem/

Your live Zomboid mods folder must receive a **full copy** of that folder after each ship.
Version alone can look right if only mod.info / KS_ModData.lua was copied.

After load, console MUST contain:
  [KnoxSystem] KS_Analyze loaded PLATE_REV=3
  [KnoxSystem] Analyze PLATE_REV=3 format=mods/HP/dmg(no [mod:] prefix) ...

If you see VERSION 0.5.55 but NOT PLATE_REV=3, the client Lua was not deployed.
Full quit PZ (not main menu) after copy — Lua is cached until process exit.

Expected plate stack (bottom near head → top):
  1) Heavy Hit - Thick Skin     (L2 mods)
  2) HP [||||||....] 60%        (L1 HP)
  3) 12.5                       (L2 damage on hit)

Mutation verifier note: empty-patch on KS_WorldZombies.lua failed earlier; harmless, not related to Analyze.
