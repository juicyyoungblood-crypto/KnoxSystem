-- Intentionally empty / no-op.
-- Previous initList override broke Item List search/filter and was not needed
-- after removing broken Knox item scripts. Icon fix is in KS_DStorage (customInventoryIcon).
print("[KnoxSystem] KS_ItemListFix loaded (noop — vanilla Item List restored)")
