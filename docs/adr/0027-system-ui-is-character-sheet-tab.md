# System UI is a character-sheet tab, not a floating window

Player-facing System progression UI is an additional tab on the vanilla character info window (alongside Info, Skills, Health, etc.), matching the design intent of a System Tab on the character sheet. A separate collapsable window and “System” chrome button are not the primary UX. Key O focuses the character sheet System tab when possible.
