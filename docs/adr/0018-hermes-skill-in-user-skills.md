# Knox System Hermes skill lives in user Hermes skills

The coordination skill is installed under the user’s Hermes skills (e.g. ~/.hermes/skills or the active profile skills), not only inside the mod repo. It loads the project design YAML, CONTEXT, and ADRs as the source of truth for plan/build/review sessions with the agent.
